rules:
  - id: trailofbits.python.pickles-in-pytorch.pickles-in-pytorch
    severity: INFO  # Downgrade severity so it’s non-blocking
    message: "Ignoring torch.load pickle warning due to reviewed and controlled usage."
    metadata:
      confidence: LOW
      ignored_reason: "torch.load used safely with controlled input."
    languages: [python]
    patterns:
      - pattern: torch.load(...)
